export default class User_Exercise {
	constructor(comment, time_in, time_out, score, code, user_id, exercise_id) {
		this.comment = comment;
		this.time_in = time_in;
		this.time_out = time_out;
		this.score = score;
		this.code = code;
		this.user_id = user_id;
		this.exercise_id = exercise_id;
	}
}
